export const API_EMAIL: string = 'amakhin@gmail.com';
export const API_PASSWORD: string = 'Omega0))';
if (!API_EMAIL || !API_PASSWORD) {
    throw new Error('API_EMAIL or API_PASSWORD is not defined');
}
